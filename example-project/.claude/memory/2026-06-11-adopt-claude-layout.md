# 2026-06-11 — Adopting the `.claude/` layout (example entry)

> Illustrative entry for the `example-project/` showcase — not live history. It
> shows the shape of a real session note.

- Adopted the typed `.claude/` layout so Claude behaves consistently across the
  team without re-explaining the stack each session.
- Decided: domain skills under `skills/`, whole-stack briefs under `context/`, and
  the flexible layers (`hooks/`, `commands/`, `agents/`, `workflows/`) scaffolded
  for later. `CLAUDE.md` stays short and points into `context/`.
- Why: keeps every session lean (only `CLAUDE.md` always-loads) while deep
  expertise stays available on demand. Empty layers left as READMEs, not
  fabricated.
- Open thread: revisit once we actually need a hook or workflow.
